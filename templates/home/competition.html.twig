{% extends 'basepublique.html.twig' %}

{% block body %}

<div class="container">
    <div class="row">
        <h4>Athletisme</h4>
        {% for competition in athletismes %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
        <div class="row">
            <h4>Basketball</h4>
            {% for competition in basketballs %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    <div class="row">
        <h4>Football</h4>
        {% for competition in footballs %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="row">
        <h4>Handball</h4>
        {% for competition in handballs %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="row">
        <h4>Judo</h4>
        {% for competition in judos %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="row">
        <h4>Lawn Tennis</h4>
        {% for competition in ltennis %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="row">
        <h4>Lutte</h4>
        {% for competition in luttes %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="row">
        <h4>Table Tennis</h4>
        {% for competition in ttennis %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="row">
        <h4>Volleyball</h4>
        {% for competition in volleyballs %}
            <div class="col s12 m3">
                <div class="card">
                    <div class="card-content">
                        <h5 class="card-title"><a href="{{ path('show_public',{id:competition.id}) }}">{{ competition.titre }}</a></h5>
                        <p>{{ competition.description }}</p>
                        <p>{{ competition.date_publication | date}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    </div>
</div>
{% endblock %}

