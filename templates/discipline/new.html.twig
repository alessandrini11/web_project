{% extends 'base.html.twig' %}

{% block title %}New Discipline{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-content">
                <span class="card-title">Ajouter Discipline</span>
                {{ include('discipline/_form.html.twig') }}
                <a href="{{ path('discipline_index') }}">back to list</a>
            </div>
        </div>
    </div>

    <script>

    </script>
{% endblock %}
