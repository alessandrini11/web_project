{% extends 'base.html.twig' %}

{% block title %}Commmentaire{% endblock %}

{% block body %}
    <h1>Commmentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commmentaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ commmentaire.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ commmentaire.email }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commmentaire.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commmentaire_index') }}">back to list</a>

    <a href="{{ path('commmentaire_edit', {'id': commmentaire.id}) }}">edit</a>

    {{ include('commmentaire/_delete_form.html.twig') }}
{% endblock %}
