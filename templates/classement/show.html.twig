{% extends 'base.html.twig' %}

{% block title %}Classement{% endblock %}

{% block body %}
    <h1>Classement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ classement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classement_index') }}">back to list</a>

    <a href="{{ path('classement_edit', {'id': classement.id}) }}">edit</a>

    {{ include('classement/_delete_form.html.twig') }}
{% endblock %}
